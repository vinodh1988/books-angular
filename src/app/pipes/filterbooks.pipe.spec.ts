import { FilterbooksPipe } from './filterbooks.pipe';

describe('FilterbooksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbooksPipe();
    expect(pipe).toBeTruthy();
  });
});
